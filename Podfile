# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'shortly' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  pod 'Alamofire', '~> 5.5'
  pod 'SwiftyJSON', '~> 4.0'

  # Pods for shortly

  target 'shortlyTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'shortlyUITests' do
    # Pods for testing
  end

end
